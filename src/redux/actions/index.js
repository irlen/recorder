//设置windowH
export const setWindowH = (windowH)=>({
  type:'SET_WH',
  windowH
})

export const setActiveKey = (activeKey)=>({
  type:'SET_ACTIVE_KEY',
  activeKey
})

export const setDeleteKey = (deleteKey)=>({
  type: 'SET_DELETE_KEY',
  deleteKey
})